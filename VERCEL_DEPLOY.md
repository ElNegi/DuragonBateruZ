# Vercel Deploy

Importa el repositorio como proyecto Vite. El comando de build es `npm run build` y el directorio de salida es `dist`. Añade el dominio de producción a Firebase Authentication Authorized domains.
