import { useState } from 'react';import { authErrorMessage,login,loginGuest,register } from '../../firebase/authService';
export function LoginScreen(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function act(mode:'login'|'register'|'guest'){setBusy(true);setError('');try{if(mode==='login')await login(email,password);else if(mode==='register')await register(email,password);else await loginGuest();}catch(e:any){console.error(e);setError(authErrorMessage(e?.code));}finally{setBusy(false)}}return <main className="screen center"><section className="auth-panel"><div className="logo-orb">気</div><h1>ECOS DE KI</h1><p>Construye tu estilo. Domina el ritmo. Sobrevive a la saga.</p><label>Correo<input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></label><label>Contraseña<input value={password} onChange={e=>setPassword(e.target.value)} type="password"/></label>{error&&<div className="error">{error}</div>}<div className="button-grid"><button onClick={()=>act('login')} disabled={busy}>Entrar</button><button onClick={()=>act('register')} disabled={busy}>Crear cuenta</button><button className="ghost" onClick={()=>act('guest')} disabled={busy}>Continuar como invitado</button></div></section></main>}
