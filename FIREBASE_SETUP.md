# Firebase Setup

El proyecto ya está configurado para `dragonlike-3f51d`. En Firebase Console habilita Authentication Email/Password y Anonymous (si se usa invitado), crea Firestore en modo producción, publica `firestore.rules`, y añade `localhost` y el dominio de Vercel a Authorized domains. No se requieren secretos de Admin SDK.
