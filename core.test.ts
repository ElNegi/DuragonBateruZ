import type { Card } from '../../cards/cardTypes';
export function CardView({card,onPlay,disabled=false}:{card:Card;onPlay?:()=>void;disabled?:boolean}){return <button className={`card card-${card.cardType.toLowerCase()}`} onClick={onPlay} disabled={disabled} title={card.rulesText}><div className="card-top"><span className="cost">{card.cost}</span><strong>{card.name}</strong></div><div className="art">✦</div><div className="tags">{[card.cardType,card.nature,card.range].filter(Boolean).join(' · ')}</div><p>{card.rulesText}</p><small>{card.rarity??'Rareza pendiente de balance'}</small></button>}
